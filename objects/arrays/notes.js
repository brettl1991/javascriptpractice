const notes = ['Note 1', 'Note 2', 'Note 3']

//console.log(notes)

//if we want to figure out how many items we have:

console.log(notes.length)

//how we can grab an individual item from the list?: with index
console.log(notes[0])
//we going to get the first one with this (above)

//if we want to get the last item from the list
console.log(notes[notes.length - 1])